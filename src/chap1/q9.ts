/**
 * 釣り合わない男女
 * 男女を並べる
 * 途中で区切る。どこで区切っても左右の男女数が同じにならない男女の並べ方の数
 */
 namespace q9 {
    const manCount = 3;
    const womanCount = 3;

    // 並べ方のバリエーションループ

    /**
     * 同数に分けられるかどうかを返す
     * @param line 男をtrue、女をfalseとする
     */
    function canSeparateEqually(line : Array<boolean>) :boolean {
        return false;
    }
 }
