/**
 * キウイジュース
 */

namespace choku {
    function thePouring(capacities: Array<number>,
                        bottles: Array<number>,
                        fromId: Array<number>,
                        toId: Array<number>): Array<number> 
    {
        let resultBottles: Array<number> = [];

        return resultBottles;
    }
}
